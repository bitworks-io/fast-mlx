CLAUDE.local.md
.agent-reports/
# Repository-local agent state and machine-owned automation files.
/.agents/
/.agent-claim
/.codex/agents/Explore.toml
/.harness-sha
/.tmp-*
# SwiftPM build artifacts (local `swift test` runs in spike/)
.build/
# Python bytecode from local unittest/projection-gate runs
__pycache__/
*.pyc
# harness runtime evidence (Task 5): generated per-run, not source
spike/harness-evidence.jsonl
spike/*.csv
# deploy-time git provenance written by private deployment tooling, not source
spike/.harness-sha
# machine-local Claude Code overrides: fleet host/IP/SSH allow-rules stay PRIVATE, never committed
.claude/settings.local.json
# machine-specific project-init customizations (e.g. CODEX_ENV_PRIVATE_OVERLAY); never commit
project-init.local
